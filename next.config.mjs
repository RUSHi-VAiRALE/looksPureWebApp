/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['placehold.co', 'clatwallah.netlify.app', 'cdn.pixabay.com', 'www.istockphoto.com','s6.imgcdn.dev'],
      },
};

export default nextConfig;
